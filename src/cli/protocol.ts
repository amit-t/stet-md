export const agentProtocol = `# Redline Agent Protocol

1. Do not delete \`redline:thread\` blocks unless Amit explicitly asks.
2. Prefer \`redline reply\` and \`redline resolve\` over manual marker edits.
3. If editing manually, add messages inside the structured marker only.
4. Use UTC ISO 8601 timestamps ending in \`Z\`.
5. Do not hand-edit generated blockquotes as source of truth.
6. If a document edit addresses a comment, reply in the thread and resolve only when complete.
`;
